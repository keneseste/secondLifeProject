/* Stili specifici per il generatore di schede di allenamento */
#generatore-schede {
    padding-top: 10rem;
    background: linear-gradient(135deg, #0a0a1a 0%, #1a1a2e 100%);
}

.schede-form {
    margin-top: 2rem;
}

.form-section {
    margin-bottom: 3rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.form-section:last-child {
    border-bottom: none;
}

.form-section h3 {
    color: #ffaa33;
    margin-bottom: 1.5rem;
    font-size: 1.4rem;
    display: flex;
    align-items: center;
}

.form-section h3 i {
    margin-right: 0.8rem;
    color: #00b7ff;
}

.section-description {
    color: #f8f9fa;
    margin-bottom: 1.5rem;
    font-size: 1.1rem;
}

.form-row {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
}

.form-group.half {
    flex: 1;
    min-width: 200px;
}

.form-group.third {
    flex: 1;
    min-width: 150px;
}

/* Stili per le giornate di allenamento */
.giornata-box {
    background-color: rgba(10, 10, 30, 0.5);
    border: 1px solid rgba(0, 183, 255, 0.2);
    border-radius: 8px;
    padding: 1.5rem;
    margin-bottom: 2rem;
    transition: all 0.3s ease;
}

.giornata-box:hover {
    border-color: rgba(0, 183, 255, 0.5);
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
}

.giornata-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.5rem;
}

.giornata-header h4 {
    color: #ffaa33;
    font-size: 1.3rem;
    margin: 0;
}

/* Stili per gli esercizi */
.esercizi-container {
    margin-top: 1.5rem;
    margin-bottom: 1.5rem;
}

.esercizi-container h5 {
    color: #00b7ff;
    font-size: 1.2rem;
    margin-bottom: 1.2rem;
    display: flex;
    align-items: center;
}

.esercizi-container h5 i {
    margin-right: 0.8rem;
}

.esercizio-box {
    background-color: rgba(10, 10, 30, 0.3);
    border: 1px solid rgba(255, 170, 51, 0.2);
    border-radius: 8px;
    padding: 1.2rem;
    margin-bottom: 1.5rem;
    transition: all 0.3s ease;
}

.esercizio-box:hover {
    border-color: rgba(255, 170, 51, 0.5);
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
}

.esercizio-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.2rem;
}

.esercizio-header h6 {
    color: #ffaa33;
    font-size: 1.1rem;
    margin: 0;
}

/* Stili per i pulsanti */
.add-button, .remove-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.3s ease;
    border-radius: 5px;
    font-weight: 600;
}

.add-button {
    background-color: #00b7ff;
    color: #fff;
    border: none;
    padding: 0.8rem 1.5rem;
    font-size: 1rem;
}

.add-button i {
    margin-right: 0.5rem;
}

.add-button:hover {
    background-color: #0095cc;
    transform: translateY(-2px);
}

.remove-button {
    background-color: transparent;
    color: #ff3a3a;
    border: 1px solid #ff3a3a;
    width: 30px;
    height: 30px;
    font-size: 0.9rem;
}

.remove-button:hover {
    background-color: rgba(255, 58, 58, 0.1);
    transform: scale(1.1);
}

.add-esercizio {
    margin-top: 0.5rem;
    background-color: rgba(0, 183, 255, 0.2);
    color: #00b7ff;
    border: 1px solid #00b7ff;
    font-size: 0.9rem;
    padding: 0.6rem 1.2rem;
}

.add-esercizio:hover {
    background-color: rgba(0, 183, 255, 0.3);
}

/* Stili per i pulsanti principali */
.form-actions {
    margin-top: 3rem;
    text-align: center;
}

.form-header-actions {
    margin-top: 0;
    margin-bottom: 1.5rem;
    text-align: right;
}

#generate-pdf {
    background-color: #ff3a3a;
    font-size: 1.2rem;
    padding: 1rem 2.5rem;
    display: inline-flex;
    align-items: center;
}

#generate-pdf i {
    margin-right: 0.8rem;
    font-size: 1.3rem;
}

.action-button {
    background-color: transparent;
    color: #00b7ff;
    border: 1px solid #00b7ff;
    padding: 0.6rem 1.2rem;
    font-size: 0.9rem;
    border-radius: 5px;
    cursor: pointer;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    font-weight: 600;
}

.action-button i {
    margin-right: 0.5rem;
}

.action-button:hover {
    background-color: rgba(0, 183, 255, 0.1);
    transform: translateY(-2px);
}

/* Stile per il pulsante template */
.template-button {
    background-color: #00b7ff;
    color: #fff;
    border: none;
    padding: 0.8rem 1.5rem;
    font-size: 1rem;
    border-radius: 5px;
    cursor: pointer;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    margin-top: 1.5rem;
    font-weight: 600;
}

.template-button i {
    margin-right: 0.8rem;
}

.template-button:hover {
    background-color: #0095cc;
    transform: translateY(-2px);
}

/* Stili per i pulsanti di duplicazione */
.duplicate-button {
    background-color: transparent;
    color: #00b7ff;
    border: 1px solid #00b7ff;
    width: 30px;
    height: 30px;
    font-size: 0.9rem;
    border-radius: 5px;
    cursor: pointer;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    margin-left: 0.5rem;
}

.duplicate-button:hover {
    background-color: rgba(0, 183, 255, 0.1);
    transform: scale(1.1);
}

/* Animazioni */
@keyframes pulse {
    0% {
        transform: scale(1);
    }
    50% {
        transform: scale(1.05);
    }
    100% {
        transform: scale(1);
    }
}

#generate-pdf:hover {
    animation: pulse 1.5s infinite;
}

/* Stili per i messaggi di errore */
.error-message {
    color: #ff3a3a;
    font-size: 0.9rem;
    margin-top: 0.3rem;
    font-weight: 500;
    display: flex;
    align-items: center;
}

.error-message::before {
    content: "⚠️";
    margin-right: 0.5rem;
}

input.error, select.error, textarea.error {
    border-color: #ff3a3a !important;
    box-shadow: 0 0 0 1px #ff3a3a;
}

/* Stili per l'anteprima della scheda (usata per il PDF) */
#scheda-preview {
    background-color: #0a0a1a;
    color: #f8f9fa;
    padding: 20px;
    font-family: 'Open Sans', sans-serif;
    max-width: 800px;
    margin: 0 auto;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
}

#scheda-preview .scheda-header {
    text-align: center;
    margin-bottom: 30px;
    padding: 30px 0;
    border-bottom: 2px solid #ffaa33;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 60vh;
}

#scheda-preview .scheda-header img {
    max-width: 200px;
    margin-bottom: 25px;
}

#scheda-preview .scheda-header h1 {
    color: #ffaa33;
    font-size: 32px;
    margin-bottom: 30px;
    text-transform: uppercase;
    letter-spacing: 1px;
}

#scheda-preview .scheda-info {
    margin: 20px auto;
    padding: 20px;
    background-color: rgba(0, 183, 255, 0.1);
    border-radius: 8px;
    border-left: 4px solid #00b7ff;
    width: 80%;
}

#scheda-preview .scheda-info p {
    margin: 8px 0;
    font-size: 16px;
}

#scheda-preview .scheda-info strong {
    color: #00b7ff;
    font-weight: 600;
}

#scheda-preview .giornata-section {
    margin-bottom: 30px;
    flex: 1;
}

#scheda-preview .giornata-title {
    background-color: #1a1a2e;
    padding: 12px 15px;
    margin-bottom: 20px;
    border-left: 5px solid #ffaa33;
    font-weight: bold;
    font-size: 20px;
    color: #ffaa33;
    border-radius: 0 8px 8px 0;
    box-shadow: 0 3px 6px rgba(0, 0, 0, 0.2);
}

#scheda-preview .esercizi-list {
    padding: 0 10px;
}

#scheda-preview .esercizio-item {
    margin-bottom: 20px;
    padding: 15px;
    background-color: rgba(10, 10, 30, 0.5);
    border: 1px solid rgba(255, 170, 51, 0.3);
    border-radius: 8px;
    transition: all 0.3s ease;
}

#scheda-preview .esercizio-nome {
    font-weight: bold;
    color: #ffaa33;
    font-size: 18px;
    margin-bottom: 10px;
    padding-bottom: 8px;
    border-bottom: 1px solid rgba(255, 170, 51, 0.2);
}

#scheda-preview .esercizio-dettagli {
    display: flex;
    flex-wrap: wrap;
    gap: 15px;
    margin: 12px 0;
}

#scheda-preview .esercizio-dettagli span {
    background-color: rgba(0, 183, 255, 0.15);
    padding: 6px 12px;
    border-radius: 5px;
    font-size: 15px;
    border-left: 3px solid #00b7ff;
}

#scheda-preview .esercizio-dettagli strong {
    color: #00b7ff;
    font-weight: 600;
}

#scheda-preview .esercizio-note {
    margin-top: 10px;
    font-size: 15px;
    color: #cccccc;
    padding: 8px 12px;
    background-color: rgba(255, 255, 255, 0.05);
    border-radius: 5px;
    border-left: 3px solid rgba(255, 170, 51, 0.5);
}

#scheda-preview .esercizio-video {
    margin-top: 10px;
    font-size: 14px;
}

#scheda-preview .esercizio-video a {
    color: #00b7ff;
    text-decoration: none;
    word-break: break-all;
}

#scheda-preview .page-footer {
    margin-top: auto;
    padding-top: 15px;
    width: 100%;
}

#scheda-preview .scheda-footer {
    border-top: 2px solid #00b7ff;
    text-align: center;
    font-size: 14px;
    color: #cccccc;
    padding: 10px 0;
}

/* Responsive */
@media (max-width: 768px) {
    .form-row {
        flex-direction: column;
        gap: 1.5rem;
    }
    
    .giornata-header, .esercizio-header {
        flex-direction: column;
        gap: 1rem;
        align-items: flex-start;
    }
    
    .remove-button {
        align-self: flex-end;
    }
    
    .esercizio-dettagli {
        flex-direction: column;
        gap: 5px;
    }
}
